package com.example.bot.FormFillerBot.util;

public enum UserState {
    START,
    AWAITING_CONSENT,
    AWAITING_NAME,
    AWAITING_BIRTH_DATE,
    AWAITING_GENDER,
    AWAITING_PHOTO,
    COMPLETED
}
